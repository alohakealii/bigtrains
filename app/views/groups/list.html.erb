<div class="group-list">
	<% @groups.each do |group| %>
		<div class="group-root">
			<%= render group %>
		</div>
	<% end %>
</div>

<% if @user %>
<div class="hidden primary-group-id"><%= @user.primary_group.id %></div>
<% end %>