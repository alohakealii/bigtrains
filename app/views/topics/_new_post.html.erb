<%= form_for [@topic, @topic.posts.build] do |f| %>
  <div class="field">
    <%= f.text_area :body %>
  </div>
  <%= f.hidden_field :topic_id %>
  <%= f.hidden_field :user_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
